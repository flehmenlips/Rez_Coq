# Required environment variables (DO NOT PUT ACTUAL VALUES HERE)
NODE_ENV=production

# Database (Get these from Render.com -> Database -> Connect)
DATABASE_URL=postgres://your-db-user:your-db-password@your-db-host/your-db-name

# Session (Generate this in Render.com -> Web Service -> Environment)
SESSION_SECRET=your-session-secret

# Email Settings (Configure in Render.com -> Web Service -> Environment)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_APP_PASSWORD=your-app-specific-password

# Notes:
# - SESSION_SECRET should be a 64+ character random string
# - Store actual values in password manager
# - Never commit real secrets to git 

# Admin Account
ADMIN_EMAIL=admin@restaurant.com
ADMIN_PASSWORD=secure-admin-password